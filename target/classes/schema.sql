-- create table CAR (
--   car_id int GENERATED BY DEFAULT AS IDENTITY primary key,
--   brand varchar(150),
--   model varchar(200),
--   power double precision,
--   year_of_issue date,
--   assessed_value dec(20)
-- );
--
-- create table AIRPLANE (
--   airplane_id int GENERATED BY DEFAULT AS IDENTITY primary key,
--   brand varchar(150),
--   model varchar(200),
--   manufacturer varchar(500),
--   year_of_issue date,
--   fuelCapacity int,
--   seats int
-- );
--
-- create table EVALUATION (
--   id int GENERATED BY DEFAULT AS IDENTITY primary key,
--   amount decimal,
--   evaluationDate date,
--
--   car_id int references CAR(car_id) on delete cascade on update cascade,
--   airplane_id int references AIRPLANE(airplane_id) on delete cascade on update cascade
-- );